<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/activity_fabo_pin_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#A0D0FF"
        android:padding="8dp"
        android:text="@string/activity_fabo_virtual_service_pin_description"
        android:textSize="14sp"/>

    <LinearLayout
        android:id="@+id/activity_fabo_pin_btn_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_margin="12dp"
        android:gravity="center_horizontal"
        android:orientation="horizontal">

        <Button
            android:id="@+id/activity_fabo_pin_save_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/button_blue"
            android:text="@string/activity_fabo_virtual_service_select"
            android:textColor="#FFF"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/activity_fabo_pin_btn_layout"
        android:layout_below="@id/activity_fabo_pin_description"
        android:orientation="horizontal">
    </LinearLayout>
</RelativeLayout>
